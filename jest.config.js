module.exports = {
    testEnvironment: "node", // Ensures Jest runs in a Node.js environment
    testMatch: ["**/__tests__/**/*.js", "**/?(*.)+(spec|test).js"], // Looks for test files
    verbose: true, // Shows detailed results in the console
  };
  